// Note: This approach doesn't work in client-side code because
// SUPABASE_SERVICE_ROLE_KEY is not available in the browser
// We've moved admin operations to API routes instead

export const supabaseAdmin = null // Disabled to prevent usage
